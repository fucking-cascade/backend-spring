spring:
  application:
    name: canonization-service0
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/  #注册中心地址
  instance:
    hostname: localhost
    instance-id: localhost:8081
server:
  port: 8081
